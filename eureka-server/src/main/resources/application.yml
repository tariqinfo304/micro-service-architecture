server:
  port: 8761

spring:
  application:
    name: eureka-server

# Since this is the registry itself, it doesn't register to anyone else
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false

logging:
  level:
    com.netflix.eureka: INFO
